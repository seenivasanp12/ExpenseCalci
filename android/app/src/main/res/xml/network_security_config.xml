<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Allows cleartext HTTP to the Express backend.
      10.0.2.2 = Android emulator host (your dev machine's localhost).
      For a real device on the same Wi-Fi, add your machine's LAN IP here too.
      For production, deploy the backend to HTTPS and remove these entries.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">192.168.0.21</domain>
    </domain-config>
</network-security-config>
